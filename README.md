# Sever Center Updates

Public feed for cryptographically signed Unified Center release manifests.

This repository contains only `release.json` manifests and detached RSA signatures. It does not contain application source code, databases, credentials, or private signing keys.
